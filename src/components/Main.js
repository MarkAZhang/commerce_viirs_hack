import MapContainer from './MapContainer'

const Main = props =>
  <div className="content">
    <div className="container">
      <MapContainer />
      <MapContainer />
    </div>
  </div>

export default Main
